use std::rc::Rc;

use slint::Model;
use slint::ModelNotify;
use slint::ModelRc;
use slint::ModelTracker;

#[derive(Clone)]
pub struct CreateGalaxyController {
    
    
}

impl CreateGalaxyController{
    pub fn new() -> Self{
        Self{ }
    }
}
